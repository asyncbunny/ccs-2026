version: 2
project_name: anon

env:
  - CGO_ENABLED=1
  - CGO_LDFLAGS=-L/lib

before:
  hooks:
    - wget -nc https://github.com/CosmWasm/wasmvm/releases/download/{{ .Env.COSMWASM_VERSION }}/libwasmvm_muslc.x86_64.a -O /lib/libwasmvm_muslc.x86_64.a
    - wget -nc https://github.com/CosmWasm/wasmvm/releases/download/{{ .Env.COSMWASM_VERSION }}/libwasmvm_muslc.aarch64.a -O /lib/libwasmvm_muslc.aarch64.a
    - wget -nc https://github.com/CosmWasm/wasmvm/releases/download/{{ .Env.COSMWASM_VERSION }}/libwasmvmstatic_darwin.a -O /lib/libwasmvmstatic_darwin.a

    - curl -LO https://musl.cc/x86_64-linux-musl-cross.tgz
    - tar xf x86_64-linux-musl-cross.tgz
    - mv -n -u x86_64-linux-musl-cross /opt/musl-cross-x86_64

    - curl -LO https://musl.cc/aarch64-linux-musl-cross.tgz
    - tar xf aarch64-linux-musl-cross.tgz
    - mv -n -u aarch64-linux-musl-cross /opt/musl-cross-aarch64

builds:
  - id: anond-linux-amd64
    main: ./cmd/anond/main.go
    binary: anond
    builder: go
    gobinary: "go"
    goos:
      - linux
    goarch:
      - amd64
    env:
    - CC=/opt/musl-cross-x86_64/bin/x86_64-linux-musl-gcc
    - LD=/opt/musl-cross-x86_64/bin/x86_64-linux-musl-ld
    flags:
      - -mod=readonly
      - -trimpath
    ldflags:
      - -X github.com/cosmos/cosmos-sdk/version.Name=anon
      - -X github.com/cosmos/cosmos-sdk/version.AppName=anond
      - -X github.com/cosmos/cosmos-sdk/version.Version={{ .Version }}
      - -X github.com/cosmos/cosmos-sdk/version.Commit={{ .Commit }}
      - -X github.com/cosmos/cosmos-sdk/version.BuildTags=ledger,muslc,mainnet
      - -linkmode=external
      - -w -s
      - -extldflags "-Wl,-z,muldefs -static -z noexecstack"
    tags:
      - ledger
      - muslc
      - mainnet

  - id: anond-linux-arm64
    main: ./cmd/anond/main.go
    binary: anond
    builder: go
    gobinary: "go"
    goos:
      - linux
    goarch:
      - arm64
    env:
      - CC=/opt/musl-cross-aarch64/bin/aarch64-linux-musl-gcc
      - LD=/opt/musl-cross-aarch64/bin/aarch64-linux-musl-ld
    flags:
      - -mod=readonly
      - -trimpath
    ldflags:
      - -X github.com/cosmos/cosmos-sdk/version.Name=anon
      - -X github.com/cosmos/cosmos-sdk/version.AppName=anond
      - -X github.com/cosmos/cosmos-sdk/version.Version={{ .Version }}
      - -X github.com/cosmos/cosmos-sdk/version.Commit={{ .Commit }}
      - -X github.com/cosmos/cosmos-sdk/version.BuildTags=ledger,muslc,mainnet
      - -linkmode=external
      - -w -s
      - -extldflags "-Wl,-z,muldefs -static -z noexecstack"
    tags:
      - ledger
      - muslc
      - mainnet

  - id: anond-darwin
    main: ./cmd/anond/main.go
    binary: anond
    builder: go
    gobinary: "go"
    goos:
      - darwin
    goarch:
      - arm64
      - amd64
    env:
      - CC=oa64-clang
      - CGO_CFLAGS=-mmacosx-version-min=10.12
      - CGO_LDFLAGS=-L/lib -mmacosx-version-min=10.12
    flags:
      - -mod=readonly
      - -trimpath
    ldflags:
      - -X github.com/cosmos/cosmos-sdk/version.Name=anon
      - -X github.com/cosmos/cosmos-sdk/version.AppName=anond
      - -X github.com/cosmos/cosmos-sdk/version.Version={{ .Version }}
      - -X github.com/cosmos/cosmos-sdk/version.Commit={{ .Commit }}
      - -X github.com/cosmos/cosmos-sdk/version.BuildTags=ledger,static_wasm,mainnet
      - -linkmode=external
      - -w -s
    tags:
      - ledger
      - static_wasm
      - mainnet

  - id: anond-testnet-linux-amd64
    main: ./cmd/anond/main.go
    binary: anond
    builder: go
    gobinary: "go"
    goos:
      - linux
    goarch:
      - amd64
    env:
      - CC=/opt/musl-cross-x86_64/bin/x86_64-linux-musl-gcc
      - LD=/opt/musl-cross-x86_64/bin/x86_64-linux-musl-ld
    flags:
      - -mod=readonly
      - -trimpath
    ldflags:
      - -X github.com/cosmos/cosmos-sdk/version.Name=anon
      - -X github.com/cosmos/cosmos-sdk/version.AppName=anond
      - -X github.com/cosmos/cosmos-sdk/version.Version={{ .Version }}
      - -X github.com/cosmos/cosmos-sdk/version.Commit={{ .Commit }}
      - -X github.com/cosmos/cosmos-sdk/version.BuildTags=ledger,muslc,testnet
      - -linkmode=external
      - -w -s
      - -extldflags "-Wl,-z,muldefs -static -z noexecstack"
    tags:
      - ledger
      - muslc
      - testnet

  - id: anond-testnet-linux-arm64
    main: ./cmd/anond/main.go
    binary: anond
    builder: go
    gobinary: "go"
    goos:
      - linux
    goarch:
      - arm64
    env:
      - CC=/opt/musl-cross-aarch64/bin/aarch64-linux-musl-gcc
      - LD=/opt/musl-cross-aarch64/bin/aarch64-linux-musl-ld
    flags:
      - -mod=readonly
      - -trimpath
    ldflags:
      - -X github.com/cosmos/cosmos-sdk/version.Name=anon
      - -X github.com/cosmos/cosmos-sdk/version.AppName=anond
      - -X github.com/cosmos/cosmos-sdk/version.Version={{ .Version }}
      - -X github.com/cosmos/cosmos-sdk/version.Commit={{ .Commit }}
      - -X github.com/cosmos/cosmos-sdk/version.BuildTags=ledger,muslc,testnet
      - -linkmode=external
      - -w -s
      - -extldflags "-Wl,-z,muldefs -static -z noexecstack"
    tags:
      - ledger
      - muslc
      - testnet

  - id: anond-testnet-darwin
    main: ./cmd/anond/main.go
    binary: anond
    builder: go
    gobinary: "go"
    goos:
      - darwin
    goarch:
      - arm64
      - amd64
    env:
      - CC=oa64-clang
      - CGO_CFLAGS=-mmacosx-version-min=10.12
      - CGO_LDFLAGS=-L/lib -mmacosx-version-min=10.12
    flags:
      - -mod=readonly
      - -trimpath
    ldflags:
      - -X github.com/cosmos/cosmos-sdk/version.Name=anon
      - -X github.com/cosmos/cosmos-sdk/version.AppName=anond
      - -X github.com/cosmos/cosmos-sdk/version.Version={{ .Version }}
      - -X github.com/cosmos/cosmos-sdk/version.Commit={{ .Commit }}
      - -X github.com/cosmos/cosmos-sdk/version.BuildTags=ledger,static_wasm,testnet
      - -linkmode=external
      - -w -s
    tags:
      - ledger
      - static_wasm
      - testnet
archives:
  - id: zipped
    builds:
      - anond-linux-amd64
      - anond-linux-arm64
      - anond-darwin
    name_template: "{{.ProjectName}}-{{ .Version }}-{{ .Os }}-{{ .Arch }}"
    format: tar.gz
    wrap_in_directory: false
    files:
      - none*
  - id: zipped-testnet
    builds:
      - anond-testnet-linux-amd64
      - anond-testnet-linux-arm64
      - anond-testnet-darwin
    name_template: "{{.ProjectName}}-testnet-{{ .Version }}-{{ .Os }}-{{ .Arch }}"
    format: tar.gz
    wrap_in_directory: false
    files:
      - none*
  - id: binaries
    builds:
      - anond-linux-amd64
      - anond-linux-arm64
      - anond-darwin
    name_template: "{{.ProjectName}}-{{ .Version }}-{{ .Os }}-{{ .Arch }}"
    format: binary
    wrap_in_directory: false
    files:
      - none*
  - id: binaries-testnet
    builds:
      - anond-testnet-linux-amd64
      - anond-testnet-linux-arm64
      - anond-testnet-darwin
    name_template: "{{.ProjectName}}-testnet-{{ .Version }}-{{ .Os }}-{{ .Arch }}"
    format: binary
    wrap_in_directory: false
    files:
      - none*

checksum:
  name_template: "{{ .ProjectName }}_{{ .Version }}_checksums.txt"
  algorithm: sha256

release:
  github:
    owner: anon-org
    name: anon
  # If PRERELEASE env var is set to "true", mark as prerelease
  # Otherwise auto-detect based on tag suffix (-rc, -beta, etc.)
  prerelease: '{{ if eq .Env.PRERELEASE "true" }}true{{ else }}auto{{ end }}'

# Docs: https://goreleaser.com/customization/changelog/
changelog:
  disable: true

dist: dist

git:
  prerelease_suffix: "-rc"
